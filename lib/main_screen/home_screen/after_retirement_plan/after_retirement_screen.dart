import 'package:flutter/material.dart';

class AfterRetirementScreen extends StatelessWidget {
  const AfterRetirementScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Column(
          children: [Row()],
        ),
      ),
    );
  }
}
